# Repository Identity

- Canonical name: Nudge
- Purpose: React Native/Expo Android social interaction prototype with friend vibe checks and availability broadcasting.
- Remote: `leafedbug-cmd/nudge`
- Required base branch: `main`; recovery branch: `agent/normalize-nudge-files`
- Recovery commit: `075c5b3` (`Save CRLF line-ending updates`)
- Entry point: `App.js`
- Manifest: `package.json`; Expo config: `app.json`
- Install/run: `npm install`; `npx expo start`
- Generated artifacts: `node_modules/` and `.expo/`; ignored.

Search terms: Nudge Vibe Check Bat Signal React Native Expo Android neon cyan.
